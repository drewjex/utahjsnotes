1. Because it's JSX and not HTML - className is a property on a div DOM element. That's why.
- DOM elements have attributes and properties. attributes are part of the html tag, properties are only accessible in JS.
2. 